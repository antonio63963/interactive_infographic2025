mixin bigNumWithTitle(options)
  -const {title, subtitle, num} = options || {};
  section.big-num-with-title 
    block
    .col 
      .amount= num
      h2.title= title
      span.subtitle= subtitle