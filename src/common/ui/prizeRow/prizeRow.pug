mixin prizeRow(options)
  -const {person, jobTitle, subtitle} = options || {};
  .prize-row(class!= attributes.class)
    .person= person 
    .job-title-wrapper 
      p
        if(jobTitle != null)
          span.job-title= jobTitle 
        if(subtitle != null)
          span.subtitle= subtitle
